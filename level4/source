#include <stdio.h>

int m = 0;

void main(void)
{
    n();
    return;
}

void n(void)
{
    char buffer[520];

    fgets(buffer, 512, stdin);
    p(buffer);
    if (m == 16930116) {
        system("/bin/cat /home/user/level5/.pass");
    }
    return;
}

void p(char *s)
{
    printf(s);
    return;
}